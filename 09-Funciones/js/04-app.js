const numero1 = 20;
const numero2 = "20";

console.log( parseInt(numero2) ); // Esto es una Función

console.log( numero1.toString() ); // Esto es un Método

// Función y metodo funcionan igual, ambos pueden ser reutilizados