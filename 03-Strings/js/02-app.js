const producto = 'Monitor 20 Pulgadas';


// Conocer la cantidad de caracteres que tiene la cadena de texto
console.log(producto.length);

// Conocer la posicion de la palabra
console.log(producto.indexOf('Pulgadas'));

// Existe la palabra que buscamos (true o false)
console.log(producto.includes('Monitor'));