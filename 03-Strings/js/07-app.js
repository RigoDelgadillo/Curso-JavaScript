const producto = 'Monitor 20 Pulgadas';

// Transforma toda la cadena de texto a Mayúsculas
console.log(producto.toUpperCase());

// Transforma toda la cadena de texto a Minúsculas
console.log(producto.toLowerCase());

const correo = 'cOrreO@CorReo';

// Escribir correo a minúscula
// Sirve para cuando envíes un correo a una base de datos evites que se dupliquen los correos por una mala redacción del usuario
console.log(correo.toLowerCase());