

let resultado;

// PI
resultado = Math.PI;

// Redondear
resultado = Math.round(2.5);
resultado = Math.round(2.6);
resultado = Math.round(2.8);
resultado = Math.round(2.3);

// Redondea hacia arriba
resultado = Math.ceil(2.1);
resultado = Math.ceil(2.9);

// Redondear hacia abajo
resultado = Math.floor(2.9);
resultado = Math.floor(2.2);

//Raiz cuadrada
resultado = Math.sqrt(144);

// Potencia
resultado = Math.pow(2,4);

// Valor Absoluto
resultado = Math.abs(-20);

// Minimo
resultado = Math.min(9, 10, 2, -3)

// Maximo
resultado = Math.max(9, 10, 2, -3)

// Aleatorio
resultado = Math.random() * 30;

// Aleatorio dentro de un rango...
resultado = Math.round( Math.random() * 30 );


console.log(resultado);