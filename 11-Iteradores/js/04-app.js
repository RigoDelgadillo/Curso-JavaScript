// While

let i = 1; // Se declara la variable

while ( i <= 10) { // Condición

    if( i % 2 === 0) {
        console.log(`${i} Par`);
    } else {
        console.log(`${i} Impar`);
    }
    
    i++; // Incremento
}
